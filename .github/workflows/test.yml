name: Test

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: SSH
        run: |
          mkdir -p ~/.ssh/
          echo "$SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          cat >>~/.ssh/config <<END
          Host production
            HostName $SSH_HOST
            User $SSH_USER
            IdentityFile ~/.ssh/id_rsa
            StrictHostKeyChecking no
          END
          
          mkdir -p .ddev/homeadditions/.ssh
          echo "$SSH_KEY" > .ddev/homeadditions/.ssh/id_rsa
          chmod 700 .ddev/homeadditions/.ssh
          chmod 600 .ddev/homeadditions/.ssh/id_rsa
        env:
          SSH_USER: ian
          SSH_KEY: ${{ secrets.SSH_PRIV_KEY }}
          SSH_HOST: ${{ secrets.IP }}

      - name: Set up host keys
        run: |
          mkdir -p ~/.ssh-agent/          
          echo "${{ secrets.IP }}" > ~/.ssh-agent/known_hosts          
          chmod 700 ~/.ssh-agent          
          chmod 600 ~/.ssh-agent/known_hosts          
          echo "${{ secrets.IP }}" > .ddev/homeadditions/.ssh/known_hosts
          chmod 600 .ddev/homeadditions/.ssh/known_hosts   

      - name: Setup DDEV
        uses: ddev/github-action-setup-ddev@v1

      - run: ddev describe

      - run: ddev composer install
      - run: ddev npm ci

      - run: ddev pull drush -y

      - run: |
          npm install cypress -g
          cypress install

      - run: npm run serve &

      - run: npm test
