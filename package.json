{
	"name": "11ty",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "npm run test:drupal && npm run test:eleventy",
		"test:drupal": "cypress run e2e/drupal.cy.js,e2e/drupal-authenticated.cy.js --config baseUrl=https://ianmoffitt.ddev.site",
		"test:eleventy": "cypress run e2e/eleventy.cy.js --config baseUrl=https://localhost:8080",
		"clean": "rm -rf ./dist/* && rm -rf ./.cache/*",
		"build": "npm run clean && npx @11ty/eleventy",
		"prod": "npm run clean && MODE=production npx @11ty/eleventy --quiet",
		"serve": "npm run clean && MODE=development DEBUG=Eleventy* npx @11ty/eleventy --serve"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@11ty/eleventy": "^2.0.1",
		"@11ty/eleventy-fetch": "^4.0.0",
		"dotenv": "^16.4.5",
		"jstransformer-markdown-it": "^3.0.0",
		"luxon": "^3.5.0"
	},
	"devDependencies": {
		"@jgarber/eleventy-plugin-postcss": "^1.0.1",
		"cypress": "^14.0.0",
		"cypress-map": "^1.43.0"
	}
}
