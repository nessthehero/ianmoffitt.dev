{
	"name": "11ty",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "rm -rf ./dist/ && rm -rf ./.cache/",
		"build": "npm run clean && npx @11ty/eleventy",
		"prod": "npm run clean && npx @11ty/eleventy --quiet",
		"serve": "npm run clean && DEBUG=Eleventy* npx @11ty/eleventy --serve"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
	},
	"dependencies": {
		"dotenv": "^16.4.5",
		"jstransformer-markdown-it": "^3.0.0",
		"@11ty/eleventy": "^2.0.1",
		"@11ty/eleventy-fetch": "^4.0.0"
	}
}
