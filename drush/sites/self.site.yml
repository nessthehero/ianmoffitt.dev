prod:
  host: ianmoffitt.dev
  user: ian
  root: /var/www/ianmoffitt.dev/docroot
  uri: https://ianmoffitt.dev
  paths:
    - files: sites/default/files
  command:
    sql:
      sync:
        options:
          skip-tables-list: cache,cache_*

local:
  uri: https://local.personal:8890
  root: /Users/ianmoffitt/Sites/ianmoffitt.dev/docroot/
  paths:
    - files: sites/default/files
  command:
    sql:
      sync:
        options:
          skip-tables-list: cache,cache_*
